import { ConsoleLogger } from "#infrastructure/loggers";

// Default implementation used for the ApplicationLogger-
export const $ApplicationLogger = ConsoleLogger;
export const $ApplicationDatasource = null;
